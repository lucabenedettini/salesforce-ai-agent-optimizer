interface:
  display_name: Salesforce Agent Optimizer
  short_description: Salesforce delivery with routed least-privilege and delete guardrails
  default_prompt: Use $salesforce-agent-optimizer to route Salesforce work through minimal context, configuration-first planning,
    least-privilege org inspection, explicit delete approval, package.xml generation, and validation.
metadata:
  version: 0.6.1
