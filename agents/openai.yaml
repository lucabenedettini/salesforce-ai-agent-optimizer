interface:
  display_name: "Salesforce Agent Optimizer"
  short_description: "Salesforce delivery with destructive-change guardrails"
  default_prompt: "Use $salesforce-agent-optimizer to plan, approve, implement, test, validate, package, and refresh release/API context for Salesforce work with minimal custom code, explicit delete approval, and token-efficient org inspection."
