interface:
  display_name: Salesforce Agent Optimizer
  short_description: Salesforce delivery with routed least-privilege and delete guardrails
  default_prompt: Use $salesforce-agent-optimizer to route Salesforce work through minimal context, mandatory request review,
    planning, approval, implementation decision, validation, least-privilege org inspection, explicit delete approval, and
    package.xml generation.
metadata:
  version: 1.2.3
