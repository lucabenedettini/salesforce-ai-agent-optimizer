interface:
  display_name: "Salesforce Agent Optimizer"
  short_description: "Salesforce delivery, testing, manifests, and version updates"
  default_prompt: "Use $salesforce-agent-optimizer to plan, approve, implement, test, validate, package, and refresh release/API context for a Salesforce change with minimal custom code and token-efficient org inspection."
